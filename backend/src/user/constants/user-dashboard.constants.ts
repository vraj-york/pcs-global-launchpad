/** Shown in API success `message` for `GET /users/me/dashboard`. */
export const USER_DASHBOARD_SUCCESS_MSG = 'User dashboard ready.';

/** Shown in API success `message` for `GET /users/me/analytics-context`. */
export const USER_ANALYTICS_CONTEXT_SUCCESS_MSG = 'Analytics context loaded.';
