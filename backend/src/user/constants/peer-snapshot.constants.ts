/** Shown in API success `message` for `GET /users/me/peer-snapshot`. */
export const PEER_SNAPSHOT_FETCHED_SUCCESS_MSG =
  'Peer snapshot fetched successfully.';

export const PEER_SNAPSHOT_FETCH_FAILED_MSG =
  'Failed to fetch peer snapshot. Please try again later.';

export const PEER_SNAPSHOT_FETCH_ERROR_LOG_MSG =
  'Error fetching peer snapshot for dashboard';
