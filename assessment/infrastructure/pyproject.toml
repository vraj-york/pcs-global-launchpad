[project]
name = "infrastructure"
version = "0.1.0"
description = "Infra setup for assessment module"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "aws-cdk-lib>=2.100.0",
    "constructs>=10.0.0",
    "pyyaml>=6.0",
]
