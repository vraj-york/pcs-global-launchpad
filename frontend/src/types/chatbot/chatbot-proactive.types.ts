/**
 * UI-only types for the employee proactive empty state (idle-driven phases).
 */

/** Index of the proactive “stage” shown before the user sends a message. */
export type ChatbotProactiveEmployeePhase = 0 | 1 | 2;
